import Link from "next/link"; import { CheckCircle2, Circle } from "lucide-react"; import { Card } from "@/components/ui/card"; import { PageTitle } from "@/components/app-page";
const steps=[["Complete os dados da barbearia","/configuracoes",true],["Cadastre seus serviços","/servicos",false],["Adicione seus primeiros clientes","/clientes",false],["Conecte seu WhatsApp","/whatsapp",false],["Crie sua primeira automação","/automacoes",false]];
export default function OnboardingPage(){return <><PageTitle eyebrow="PRIMEIROS PASSOS" title="VAMOS COLOCAR A VOLTTA PRA RODAR."/><Card className="max-w-2xl"><p className="text-sm text-neutral-500">Faça estes passos para começar a reconquistar clientes.</p><div className="mt-7 space-y-2">{steps.map(([label,href,done])=><Link href={href as string} key={label as string} className="flex items-center gap-3 rounded-lg p-3 hover:bg-neutral-50">{done?<CheckCircle2 className="size-5 text-[#a58450]"/>:<Circle className="size-5 text-neutral-300"/>}<span className={done?"text-neutral-400 line-through":"font-semibold"}>{label as string}</span></Link>)}</div></Card></>;}
