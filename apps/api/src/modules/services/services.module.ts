import { Body, Controller, Get, Injectable, Module, Param, Patch, Post, NotFoundException } from '@nestjs/common';
import { Type } from 'class-transformer'; import { IsInt, IsNumber, IsOptional, IsString, Min } from 'class-validator';
import { PrismaModule } from '../../prisma/prisma.module'; import { PrismaService } from '../../prisma/prisma.service'; import { AuthUser, CurrentUser } from '../../common/decorators/auth.decorators';
class ServiceDto { @IsString() name!: string; @Type(() => Number) @IsNumber() @Min(0) price!: number; @Type(() => Number) @IsInt() @Min(1) durationMinutes!: number; @IsOptional() @Type(() => Number) @IsInt() @Min(1) returnIntervalDays?: number; }
@Injectable() class ServicesService { constructor(private prisma: PrismaService) {} list(companyId: string, activeOnly = false) { return this.prisma.service.findMany({ where: { companyId, ...(activeOnly ? { isActive: true } : {}) }, orderBy: { name: 'asc' } }); } create(companyId: string, dto: ServiceDto) { return this.prisma.service.create({ data: { companyId, ...dto } }); } async update(companyId: string, id: string, dto: ServiceDto) { const result = await this.prisma.service.updateMany({ where: { id, companyId }, data: dto }); if (!result.count) throw new NotFoundException('Serviço não encontrado'); return this.prisma.service.findUniqueOrThrow({ where: { id } }); } async deactivate(companyId: string, id: string) { const result = await this.prisma.service.updateMany({ where: { id, companyId }, data: { isActive: false } }); if (!result.count) throw new NotFoundException('Serviço não encontrado'); return { message: 'Serviço desativado' }; } }
@Controller('services') class ServicesController { constructor(private service: ServicesService) {} @Get() list(@CurrentUser() u: AuthUser) { return this.service.list(u.companyId); } @Post() create(@CurrentUser() u: AuthUser, @Body() dto: ServiceDto) { return this.service.create(u.companyId, dto); } @Patch(':id') update(@CurrentUser() u: AuthUser, @Param('id') id: string, @Body() dto: ServiceDto) { return this.service.update(u.companyId, id, dto); } @Patch(':id/deactivate') deactivate(@CurrentUser() u: AuthUser, @Param('id') id: string) { return this.service.deactivate(u.companyId, id); } }
@Module({ imports: [PrismaModule], controllers: [ServicesController], providers: [ServicesService], exports: [ServicesService] }) export class ServicesModule {}
